import { DropDownHoverDirective } from './drop-down-hover.directive';

describe('DropDownHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownHoverDirective();
    expect(directive).toBeTruthy();
  });
});
